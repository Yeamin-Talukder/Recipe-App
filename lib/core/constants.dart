import 'package:flutter/material.dart';

// ─── Brand / Primary Colors ────────────────────────────────────────────────────
const Color kPrimaryColor      = Color(0xFFE8623A);   // Warm coral-orange (premium, appetizing)
const Color kPrimaryLight      = Color(0xFFF08060);   // Soft warm coral highlight
const Color kPrimaryDark       = Color(0xFFC94A25);   // Deep burnt-orange for pressed/dark states
const Color kBannerColor       = Color(0xFFE8623A);

// Gradient stops for the banner / hero elements
const Color kGradientStart     = Color(0xFFE8623A);   // Warm coral
const Color kGradientEnd       = Color(0xFFF5A623);   // Warm amber gold

// ─── Dark Theme Surfaces ───────────────────────────────────────────────────────
// Warm charcoal (brown-tinted dark) — feels cozy & food-appropriate, not "tech-dashboard"
const Color kDarkBackground    = Color(0xFF1A1410);   // Very dark warm brown-charcoal
const Color kDarkSurface       = Color(0xFF231E19);   // Warm dark base (AppBar, inputs)
const Color kDarkCard          = Color(0xFF2D2621);   // Rich warm charcoal card
const Color kDarkDivider       = Color(0xFF3D3530);   // Subtle warm divider
const Color kDarkNavBar        = Color(0xFF252019);   // Bottom nav warm background

// ─── Light Theme Surfaces ──────────────────────────────────────────────────────
// Warm ivory/cream — appetizing, premium, not generic off-white
const Color kBackgroundColor   = Color(0xFFFAF8F5);   // Warm ivory background
const Color kCardColor         = Colors.white;
const Color kSurfaceColor      = Color(0xFFFFF5EE);   // Warm peach surface accent

// ─── Text Colors ──────────────────────────────────────────────────────────────
// Dark mode text — warm off-white (easier on eyes under warm dark surfaces)
const Color kDarkTextPrimary   = Color(0xFFF5EFE8);   // Warm off-white
const Color kDarkTextSecondary = Color(0xFFA8968A);   // Warm muted taupe
const Color kDarkTextLight     = Color(0xFF6E5E56);   // Dark warm muted

// Light mode text — warm near-black for contrast on ivory
const Color kTextPrimary       = Color(0xFF1C1410);   // Warm near-black
const Color kTextSecondary     = Color(0xFF6B5E58);   // Warm medium brown-gray
const Color kTextLight         = Color(0xFFA49890);   // Warm light gray

// ─── Accent Colors ─────────────────────────────────────────────────────────────
const Color kSuccessColor      = Color(0xFF2ECC71);   // Fresh green (healthy/veggie feel)
const Color kFavoriteColor     = Color(0xFFE84393);   // Hot pink / save / love
const Color kWarningColor      = Color(0xFFF5A623);   // Warm amber warning
const Color kStarColor         = Color(0xFFFFBE3D);   // Rich golden star / rating

// ─── Spacing & Radius ─────────────────────────────────────────────────────────
const double kHorizontalPadding = 18.0;
const double kCardRadius        = 20.0;
const double kButtonRadius      = 16.0;
